# Vercel → Project → Settings → Environment Variables → Production
# Публичный URL API на Railway (без слэша в конце). Пусто в dev — используется прокси Vite.
VITE_API_URL="https://your-service.up.railway.app"
